# Implement sorting by title or by name

- STATUS: OPEN
- PRIORITY: 100
- TAGS: filter,search,feature

Add button-icons to sort the listing by title or by date

# date
there's already an example in the JS where the dateISO is the thing use with
```JavaScript
arr.sort((a, b) => {new Date(b.dateIso) - new Date(a.dateIso)})`
```

# title
